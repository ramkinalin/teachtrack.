<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Class reminders and event alerts.

         POST_NOTIFICATIONS is a runtime permission from Android 13 onward; the
         app asks for it the first time a reminder is switched on, not at launch.

         RECEIVE_BOOT_COMPLETED lets scheduled reminders be re-registered after a
         restart. Android drops all pending alarms on reboot, so without this a
         teacher who restarts their phone silently loses every reminder.

         Deliberately NOT requesting SCHEDULE_EXACT_ALARM or USE_EXACT_ALARM.
         Google restricts those to apps whose core function is alarms or
         calendars, and requesting them risks a Play Store rejection. Reminders
         are scheduled inexactly instead, which may fire a couple of minutes late
         — acceptable for "your next class is soon", and it keeps the app
         installable without a permissions justification. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:label="TeachTrack"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Delivers a reminder when its scheduled time arrives. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false"/>

        <!-- Re-registers pending reminders after a reboot or an app update.
             MY_PACKAGE_REPLACED matters as much as BOOT_COMPLETED: updating the
             app also clears its alarms. The two QUICKBOOT actions cover HTC and
             some older Chinese ROMs that never send the standard boot action. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>

        <!-- Required from Android 11 (API 30) for the contact links in the home
             screen footer. Without these declarations `canLaunchUrl` reports
             false and the links silently do nothing, because an app may no longer
             see which other apps can handle an intent unless it says what it is
             looking for.

             https covers the wa.me WhatsApp link; SENDTO with a mailto scheme
             covers the email link. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO"/>
            <data android:scheme="mailto"/>
        </intent>
    </queries>
</manifest>
